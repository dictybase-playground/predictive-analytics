{
  "name": "predictive-analytics",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "node generatePredictions",
    "export": "mongoexport --collection predictions --db guessjs_dev --out results.json --jsonArray",
    "parse": "node jsonParse",
    "predict": "npm run generate && npm run export && npm run parse"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.1.0",
    "googleapis": "^42.0.0",
    "mongoose": "^5.6.11"
  }
}
